import SwiftUI

struct WeekplanView: View {
    
    var body: some View {
        ScrollView {
            LazyVStack(spacing: 0) {
                
            }
        }
        .scrollBounceBehavior(.basedOnSize)
    }
}

#Preview {
    WeekplanView()
}
